using UnityEngine;

public class Asteroid : Obstacle
{
    // Had plans to add asteroid specific behavior but for now it just uses the base Obstacle functionality
    // Keeping this class for future extensions and to maintain clear structure
}
